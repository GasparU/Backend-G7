pip list > lista todas las librerias que tenemos instaladas
python -m venv NOMBRE > creara un entorno virtual en la ubicación donde estemos
NOMBRE/Scripts/Activate > levanta el entorno virtual en la terminal si esta es cmd, powershell o terminal windows
source NOMBRE/Scripts/Activate > levanta el entorno virtual en la terminal si esta es GIT, BASH, Terminal S.O. linux o macOS
pip freeze > lista solamente las librerias instaladas manualmente
pip freeze > requeriments.txt >> guarda el output de ese comando en el archivo requeriments.txt
pip install -r requeriments.txt > instala todas las librerias usando como fuente el archivo donde listamos todas las librerias
deactivate > desactiva el entorno virtual